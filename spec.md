# spec.md — 案件の要件

> **この1枚がこの案件の芯です。** 第1〜4節は契約（検収の基準）です。第5節はそれ以外の扱いを定めます。
> 記入は担当エージェントが行い、利用者に書かせません。

## 1. 誰の何が良くなるか

**非エンジニアでも、安心して開発を進められるようになります。**

具体的には、次の7つが起こらなくなります。

1. **AIが専門用語で説明してくる。** 用語を調べないと、返事の内容を判断できない。
2. **動いていないのに「できました」と言われる。** 自分では確かめようがないので、信じるしかない。
3. **頼んでいないものまで作られる。** 気づかないうちに話が広がっている。
4. **技術的な選択肢を並べられ、どれがいいか判断を求められる。** 判断する材料を持っていない。
5. **新しく始めるたびに、経緯を一から説明し直す。** 前回の続きから始められない。
6. **エラーなど、問題があるのか無いのか判断できない。** 画面に出ている文字が、正常なのか異常なのか読み取れない。
7. **新しい問題が次々に見つかり、その修正を繰り返して、いつまでも完成しない。** 終わりが見えない。

## 2. 検収条件

**検収条件はA群とB群に分かれます。** A群がこの案件の芯で、B群はそこへ届けるための手段です。
**B群が全部緑でも、A群が1件でも赤なら検収は通りません。** 逆は通ります（届け方は替えが利きますが、届いたものが効かなければ何も残らないためです）。

### A群 — 届いたものが、実際に効いているか

一式を入れたフォルダで、実際にAIに開発を頼んで確かめます。ブラウザの外で起きるため自動では回せません。**リリースのたびに、Claude Code / Codex / Antigravity の3つすべてで1回ずつ手で確かめます。**

- **A-1（「できました」が事実と一致する）**:
  Given 一式を入れたフォルダで、AIに小さな機能を1つ頼んでいる。
  When AIが「できました」と報告する。
  Then その報告に、**直す前に失敗する検査の出力・直した後に通る出力・戻すと同じ失敗が再現する出力**の3つが揃っている。1つでも欠けていれば赤とする。

- **A-2（頼んでいないことをしない）**:
  Given 一式を入れたフォルダで、`spec.md` の範囲の外にあることを頼んでいる。
  When AIが応答する。
  Then 勝手に作り始めず、範囲の外であることを述べて止まり、人に判断を求める。

- **A-3（毎回ゼロから説明し直さずに済む）**:
  Given 一式を入れたフォルダで、前のセッションを終えている。
  When 新しいセッションを始め、「続きをやって」とだけ言う。
  Then AIは前回の到達点と次にやることを自分で読み、こちらから経緯を説明し直さなくても続きから始める。

- **A-4（専門用語と選択肢で止めない）**:
  Given 一式を入れたフォルダで、コードを読めない人がAIとやり取りしている。
  When AIが説明・報告・確認を返す。
  Then 専門用語をそのまま出さず（出すときは定義を添え）、技術的な選択肢を並べて選ばせない。何かを求めるときは、推奨を含む3つの選択肢と、自由に書ける余地を必ず添える。白紙で問いを投げない。説明の長さは問わないが、読んで分かることを優先する。

- **A-5（問題があるのか無いのかが、ひと目で分かる）**:
  Given 一式を入れたフォルダで、AIが作業を終えて報告している。
  When コードを読めない人がその報告を読む。
  Then **1行目に「問題なし」か「要対応」のどちらかが書かれている。** 要対応なら、何が起きているかが専門用語なしで書かれている。判断のためにログや出力を読ませない。

- **A-6（際限なく直し続けて終わらない、が起きない）**:
  Given 一式を入れたフォルダで、頼んだ作業の途中でAIが別の問題を見つけた。
  When AIがその問題をどう扱うか決める。
  Then **その場で直さず、あと回しとして記録に残し、いま頼まれたことを先に終わらせる。** 直すかどうかは、終わってから利用者が決める。見つけたことを黙って握りつぶすことも、その場で直し始めることも、どちらも赤とする。

- **A-7（人のファイルを勝手に潰さない）**:
  Given すでにファイルが入っているフォルダで、一式を入れようとしている。
  When AIが指示書のとおりに進める。
  Then 同じ名前のものがある時点で書き込まずに止まり、人に確認を求める。既存のファイルは1つも変わらない。

### B群 — 一式が、正しく手元に届くか

こちらは通し操作として自動で回せます。**この節の Given / When / Then が、そのまま E2E のハッピーパスになります。** 転写の手順と雛形は [`templates/e2e/README.md`](./templates/e2e/README.md) を参照してください。

- **B-1（受け取れる）**:
  Given 何も準備していない人が、配布された画面をブラウザで開いている。
  When 作りたいものを自分の言葉で数行書いて「用意する」を押す。
  Then Markdownファイルが1個だけ自動でダウンロードされ、その中に「書いた文」「一式を取得する手順」「取得したものを照合する手順」「上書きを禁じる指示」が入っており、画面には「次にAIへこう言ってください」の1文が表示されて、それをコピーできる。

- **B-2（何度でも使い回せる）**:
  Given 一度ダウンロードした人が、同じ画面を開いている。
  When 別の作りたいものを書いて「用意する」を押す。
  Then もう1個のファイルがダウンロードされ、先にダウンロードしたファイルは消えても変わってもいない。

- **B-3（壊れたものを掴ませない）**:
  Given 置き場のファイルを1バイト書き換えてある。
  When 指示書のとおりに取得と照合を行う。
  Then 照合が一致せずに止まり、ファイルは1つも置かれない。

- **B-4（空のまま配らない）**:
  Given 作りたいものを1文字も書いていない状態で、画面を開いている。
  When 「用意する」を押す。
  Then ダウンロードは始まらず、「何を作りたいか、数行書いてください」と画面に表示される。

## 3. スコープ境界

- **契約に含む**:
  - 作りたいことを自分の言葉で書ける入力画面
  - 書いた内容から、**Markdownファイル1個**を組み立てること。そのファイルは、AIに読ませる指示書であり、次の4つを含む
    1. 利用者が書いた「作りたいもの」
    2. **一式を取得するコマンド1本**（既存の仕組みに乗る。自作しない）
    3. **取得したものを検証するコマンド1本**、および検証が通らなければ完了と言ってはならない旨
    4. **既にあるファイルを上書きせず、その場で止めて利用者に確認させる指示**

  - **一式の中身をAIに書かせないこと。写させもしないこと。** AIにやらせるのは取得と実行だけにする。生成させると内容が劣化し、しかも成功しているように見える（LEVEL B 基本原則 3 Reuse First）
  - **正しく置かれたかの判定を、AIの判断に委ねないこと。** 合否はハッシュ値の一致で決める（LEVEL B 基本原則 9 Mechanical Check First）。自作の照合処理を書かず、OSに元から入っている手段だけを使う
    - 取得: `curl`（Windows 10 以降・macOS に標準で入っている）
    - 照合: `certutil -hashfile`（Windows）/ `shasum -a 256`（macOS・Linux）。**どれも追加インストールを必要としない**
  - **正解のハッシュ値を、ダウンロードされる Markdown の中に書いておくこと。** 取得物と一緒に届いた値で取得物を検証しても意味がない。Markdown は利用者が自分でこちらの画面から受け取ったものであり、そこが信頼の起点になる
  - **利用者に、追加のアカウント登録・サービス登録を一切要求しないこと。** 置き場はこちらのサイトとし、第三者のサービス名を顧客の前に出さない。`git` を顧客側の前提条件にしない（Windows には標準で入っていないため）
  - **利用者にコマンドを打たせないこと。** 打つのはAIであり、利用者の操作はコピーと貼り付けだけに留める
  - **検証のためのスクリプトを一式に同梱し、完了報告の前に必ず実行させること。** 既存の `scripts/` と同じ土俵（bash と grep のみ）に置く
  - **その検証が本当に落ちることを、出荷前に証明すること。** わざと1バイト壊した配布物を食わせ、検証が赤になることを確認する（AGENTS.md D-9 の赤／緑／戻して赤）。常に緑を返す検査は、無いより有害である。第2節 B-3 がこれにあたる
  - **一式の中身そのもの。** 届いた一式が A群を満たすように、守らせるルール・案件の芯を書く1枚・引き継ぎの置き場・検査の雛形を用意すること。**配り方ではなく、ここがこの案件の本体である**
  - そのファイルを、ボタン1つで自動的にダウンロードさせること（保存先を選ばせない）
  - 「次にAIへこう言えばよい」の1文の提示
  - **Claude Code / Codex / Antigravity の3つすべてで、開発側の環境で1回ずつ A群を実際に確かめること。**「どれか1つで動いた」では済ませない（AGENTS.md 避けるべき未来5位）。この確認はリリースのたびに行う
  - **顧客の環境では成否を確かめられないため、失敗したときに黙って半端な状態にならず、その場で止まって理由が見えること**
  - 相手のパソコンに何も入れさせずに使えること（配布先で追加の導入作業を発生させない）

- **含まない**:
  - AIエージェント本体の導入・設定・契約（すでに動かせている前提）
  - **`git` や GitHub アカウントを顧客に用意させること**（開発側では使いますが、顧客には一切要求しません）
  - **こちらの画面が、利用者のパソコンにファイルを作ること**（作るのはAIです）
  - **自作の完全性チェック**（独自のハッシュ計算、ファイル数の目視照合、AIによる自己申告の照合）
  - 作られた一式を使って、実際のアプリを作りきること（それは各案件の仕事）
  - 出来上がったアプリの公開・運用
  - 利用者の登録・ログイン・料金の受け取り
  - 上記3つ以外のAIエージェントへの対応
  - **顧客ごとの環境で、渡すたびに動作を確かめること**（こちらから確かめる手段がありません。だから「失敗が見える形で止まる」ほうで担保します）
  - 配るファイルを2個以上に分けること（フォルダ・圧縮ファイル・複数ファイルは配りません）

- **追加費用になる**:
  - 「含まない」に挙げた項目を、あとから足す場合
  - 指示書の中身を、案件の分野ごとに作り分ける場合

## 4. 停止4領域の所在

この案件で **金額 / 日付 / 権限 / データの削除・上書き** を扱う箇所です。該当が無い領域には「なし」と書きます。
（この4領域では、想定外の値をそのまま返さずその場で停止します。停止すれば通し操作が完了しなくなり、第2節の検収条件が捕まえます。）

- **金額**: なし。この製品はお金を扱いません。
- **日付**: なし。生成の日時を記録する場合も、判断には使いません。
- **権限**: なし。ログインを持たず、利用者を区別しません。
- **データの削除・上書き**: **あり。** ただし書き込むのはこちらの画面ではなく、指示書を読んだAIです。**指示書には、既に同じ名前のファイルやフォルダがある場合は上書きせず、その場で止めて利用者に確認する、という指示を必ず入れます。** これを入れ忘れると、AIが利用者の既存ファイルを黙って潰します。第2節の A-7 と B-1 がこれを捕まえます。
  こちらの画面自体は、利用者のファイルに一切触れません（保存先を選ばせず、ダウンロードとして渡すだけです）。

### 保護対象ルート一覧

未ログインの利用者が到達できてはいけない URL・エンドポイントを列挙します。この一覧は破滅的脆弱性の検査スクリプトが入力として読み取るため、形式を守ってください。

形式: この見出しの直下に **1行1ルート**で `- <HTTPメソッド> <パス>` と書きます。メソッドは大文字、パスは `/` で始めます。注釈・空行・入れ子の箇条書きを混ぜません。該当が無い場合は `- NONE` の1行だけを書きます。

この案件は認証を持たず、保護対象のルートがありません。

- NONE

## 5. 可変層の宣言

第1〜4節以外は、実装側の裁量で変えてよい範囲です。画面の構成・技術選定・ファイル配置・実装の順序について、利用者への確認は行いません。

期待値が変わったときは第1〜4節を更新し、更新した内容を記録します。**更新は失敗ではありません。** 作ってみて分かったことの結果です。
