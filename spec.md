# spec.md — 案件の要件

> **この1枚がこの案件の芯です。** 第1〜4節は契約（検収の基準）です。第5節はそれ以外の扱いを定めます。
> 記入は担当エージェントが行い、利用者に書かせません。

## 1. 誰の何が良くなるか

AIに開発を任せ始めたばかりでコードを読めない人が、毎回ゼロから頼み方を手探りしなくても、欲しかったものが動く状態で受け取れるようになります。

## 2. 検収条件

**この節に書いた通し操作は、そのまま E2E のハッピーパスになります。** 転写の手順と雛形は [`templates/e2e/README.md`](./templates/e2e/README.md) を参照してください。

- **検収条件1（初めての人が、一式を受け取れる）**:
  Given 何も準備していない人が、配布された画面をブラウザで開いている。
  When 作りたいものを自分の言葉で数行書いて「用意する」を押し、保存先のフォルダを選ぶ。
  Then 選んだフォルダにAIへ渡す一式が書き出され、画面に「次にAIへこう言ってください」の1文が表示され、それをコピーできる。

- **検収条件2（何度でも使い回せる）**:
  Given 一度フォルダを用意した人が、同じ画面を開いている。
  When 別の作りたいものを書いて「用意する」を押し、前とは違うフォルダを選ぶ。
  Then 新しいフォルダにも一式が書き出され、前に用意したフォルダの中身は変わっていない。

- **検収条件3（人のファイルを消さない）**:
  Given すでにファイルが入っているフォルダを、保存先として選ぼうとしている。
  When 「用意する」を押して、そのフォルダを選ぶ。
  Then 書き出しの前に「このフォルダにはすでにファイルがあります」と表示されて処理が止まり、フォルダの中身は1つも変わらない。

## 3. スコープ境界

- **契約に含む**:
  - 作りたいことを自分の言葉で書ける入力画面
  - 書いた内容から、AIに渡す一式（守らせるルール／案件の芯を書く1枚／受入検査の雛形）を組み立てること
  - 選んだフォルダへの書き出しと、上書きになる場合の停止
  - 「次にAIへこう言えばよい」の1文の提示
  - Claude Code / Codex / Antigravity のどれで開いても同じものが読める形にすること
  - 相手のパソコンに何も入れさせずに使えること（配布先で追加の導入作業を発生させない）

- **含まない**:
  - AIエージェント本体の導入・設定・契約（すでに動かせている前提）
  - 書き出した一式を使って、実際のアプリを作りきること（それは各案件の仕事）
  - 出来上がったアプリの公開・運用
  - 利用者の登録・ログイン・料金の受け取り
  - 上記3つ以外のAIエージェントへの対応

- **追加費用になる**:
  - 「含まない」に挙げた項目を、あとから足す場合
  - 書き出す一式の中身を、案件の分野ごとに作り分ける場合

## 4. 停止4領域の所在

この案件で **金額 / 日付 / 権限 / データの削除・上書き** を扱う箇所です。該当が無い領域には「なし」と書きます。
（この4領域では、想定外の値をそのまま返さずその場で停止します。停止すれば通し操作が完了しなくなり、第2節の検収条件が捕まえます。）

- **金額**: なし。この製品はお金を扱いません。
- **日付**: なし。生成の日時を記録する場合も、判断には使いません。
- **権限**: なし。ログインを持たず、利用者を区別しません。
- **データの削除・上書き**: **あり。** 利用者が選んだフォルダへの書き出しが該当します。**フォルダに1つでもファイルがある場合、書き出さずにその場で停止します。** 既存ファイルの上書き・削除は、いかなる条件でも行いません。第2節の検収条件3がこれを捕まえます。

### 保護対象ルート一覧

未ログインの利用者が到達できてはいけない URL・エンドポイントを列挙します。この一覧は破滅的脆弱性の検査スクリプトが入力として読み取るため、形式を守ってください。

形式: この見出しの直下に **1行1ルート**で `- <HTTPメソッド> <パス>` と書きます。メソッドは大文字、パスは `/` で始めます。注釈・空行・入れ子の箇条書きを混ぜません。該当が無い場合は `- NONE` の1行だけを書きます。

この案件は認証を持たず、保護対象のルートがありません。

- NONE

## 5. 可変層の宣言

第1〜4節以外は、実装側の裁量で変えてよい範囲です。画面の構成・技術選定・ファイル配置・実装の順序について、利用者への確認は行いません。

期待値が変わったときは第1〜4節を更新し、更新した内容を記録します。**更新は失敗ではありません。** 作ってみて分かったことの結果です。
