# AGENTS.md

> **中心思想**
> **既にあるものを探し、必要な差分だけ作り、過去の失敗に該当するときだけ照合し、重要な変更だけ独立した目で疑い、最後は実際に動いた事実で証明する。**
> *(最小の工程で、十分な品質を得る)*

---

## 🎯 この案件が向かう先 (ALWAYS-ON GOAL / 常時掲示)

> - **人間**: 迷ったら、この先の長いルールより先にここを読む。判断は「望ましい未来に近づくか／避けるべき未来のどれかに当たるか」の一問だけで足りる。
> - **AI**: 全エージェント（Claude / Codex / Antigravity）必読。**作業開始前**と**フェーズの区切りごと**に読み直し、自分がいま書いている差分を、望ましい未来1件・避けるべき未来5件・検知の原則5点に照合する。照合結果が「避けるべき未来」に触れる場合、実装を続けず先にユーザーへ報告する。

### 望ましい未来（1つだけ）

> **依頼したものが、途中を見に行かなくても、依頼どおりに動く状態で返ってくる。**

*(「依頼どおり」は固定された仕様のことではない。**期待値は常に変わる**。仕様どおりに作られ仕様どおりに動いていても、さらに良くしたいことは起こり得る。それは失敗ではなく期待値の更新である。)*

**この案件で何を作るかは [`spec.md`](./spec.md) に書く。** 第1節（誰の何が良くなるか）と第2節（検収条件）がこの案件の芯であり、実装・検収・スコープの判断はすべてそこを参照する。

**終了条件（達成／未達を判定する手続き）**
ユーザーが違和感・不満・「こうしたい」を**1件**述べてから、判断（変更する／しない）が返るまでに、**ユーザーが追加で応答を求められた回数**を数える。**0回に近いほど達成**とする。**件数・割合・平均では測らない**（理由は下の「検知の原則」）。

### 検知の原則 (DETECTION) — 3本柱の第3

> - **人間**: おかしいと感じたら、その1件がそのまま検知。理由を説明する必要も、正しいかを確かめる必要もない。直すかどうかは後で決める。
> - **AI**: 以下の5点を検知の設計・運用の不変条件とする。違反する仕組みを提案しない。

1. **検知の単位は1件。** 割合・平均・集計で測らない。1件の違和感は1件の検知として必ず立てる。集計は1件を平均に埋め、検知を殺す。
2. **参照は仕様ではなく、その時点の期待値。** 仕様どおりに作られ、仕様どおりに動いていても、期待値との差があればそれは検知である。仕様適合(conformance)の判定だけでは、この差を構造上ひとつも捕まえられない。
3. **期待値は常に変わる。** 「さらに良くしたい」は期待値の更新であって失敗ではない。参照が動くことを前提に設計する。
4. **検知 ≠ 変更。** 検知の後に、コスト・影響を含む検討があり、**「変更しない」も正当な結論**である。両者が分離しているから、検知の敷居を下げても変更は増えない。**だから1件で発火してよい。**
5. **検知を握りつぶさない。** 検討の結果として却下することはできるが、検知そのものを立てないことは許されない。却下は記録に残す。

**門は2つ。全エージェントが必ず通す。**
着手前 = [`prompts/intent-backtranslator.md`](./prompts/intent-backtranslator.md)（別AIが詳細指示だけを盲目で読み1文に戻す）。
**門①を回す時点は「新しいゴールを決めるとき」に1回だけ。セッション開始（チェックイン）では回さない。** ゴールは1案件につき最初に1回決める。
門①が読む「詳細指示」＝この案件の [`spec.md`](./spec.md)。作り方は [`project-spec` スキル](./.agents/skills/project-spec/SKILL.md)（D-1）。
完了時 = [`prompts/drift-detector.md`](./prompts/drift-detector.md)（別AIが7問に はい/いいえ）。**作業した当のAIには実行させない。**
**門②を回す時点は「変更が出来上がった直後・同じセッションの中」。セッション終了（チェックアウト）では回さない。** 検知が出たらそのセッション内で直す。

### 避けるべき未来（致命的な順に5つ）

#### ❌ 1位. 仕組みづくりが目的化し、開発そのものが止まる
- **合図**: ルール・スキル・検査は増えたのに、**動くものが1つも増えていない**。仕組みの整備に費やした時間が、納品物に費やした時間を超えている。
- **なぜ最も致命的か**: 前進している感覚があるまま何も前進していない。**自覚できない**ため、他の4つと違い自力では止まらない。

#### ❌ 2位. 「できました」が事実と違うまま量産される
- **合図**: ユーザーが実際に触った案件が動かない。完了報告に「赤（修正前に失敗する検査の出力）／緑（修正後）／戻して赤（revert で同一エラーが再現）」の3点が揃っていない。
- **なぜ致命的か**: 開発体験悪化の原因(a) そのものの再来。1件でも起きると、触っていない残り99件も信用できなくなる。

#### ❌ 3位. ユーザーの手間が案件数に比例して増える
- **合図**: 打ち手の説明に「ユーザーが確認する／判断する／選ぶ／気づく」が含まれ、その回数が案件数とともに増える。
- **なぜ致命的か**: 100並列が物理的に不可能になり、運用そのものが成立しない。**これまで却下したすべての案の共通項**である（(docs/failures.md F-20260825-03)）。

#### ❌ 4位. ルール・仕様が肥大化し、AIが逆に見失う
- **合図**: `AGENTS.md` と各案件の仕様書の行数が増え続ける。「ルールに書いてあるのに守られない」事例が出る。
- **なぜ致命的か**: 打ち手が原因に転化する。ゴール・ネグレクトの臨界要因は遂行の複雑さではなく**最初の教示の複雑さ**であり（Duncan）、長く書くほど見失いは増える。**このブロック自体もその対象**であり、短く保つ。

#### ❌ 5位. 1つのAIでしか効かない仕組みになる
- **合図**: 打ち手が特定AI固有の機構（`.claude/` のフック等）に依存している。Codex・Antigravity で同じ検査が走らない。
- **なぜ致命的か**: コストN倍・効果ゼロ。ただし発覚後に作り直せるため、上の4つより回復可能であり5位とする。

---

## 4層ルール構造 (Rule Hierarchy)

本リポジトリのルールは以下の4階層で構成される。上位レベルのルールは下位レベルに常に優先する。

```text
LEVEL A: NON-NEGOTIABLE RULES     (絶対遵守・省略不可)
LEVEL B: DEVELOPMENT PRINCIPLES    (開発思想・基本Harness)
LEVEL C: RISK-BASED WORKFLOW      (リスク別動的プロセス・Sub-Agent)
LEVEL D: REPOSITORY-SPECIFIC RULES (プロジェクト固有ルール)
```

---

# LEVEL A — NON-NEGOTIABLE RULES (絶対ルール)

リスクレベルや作業規模にかかわらず、いかなる場合も省略・妥協してはならない。

1. **秘密情報の保護**
   - APIキー、トークン、パスワード、秘密鍵、本番接続文字列、認証情報などのシークレットをコード内に生成・表示・コミット・受領しない。
2. **個人情報の保護**
   - 実在する個人情報、顧客データをサンプルやテストデータ、ログに使用しない。不必要に外部へ送信しない。
3. **権利・素材の保護**
   - 著作権不明なコード、画像、音源、ライブラリ、外部データを無断混入しない。
4. **品質検査の完全性**
   - テストを勝手に削除・弱体化させてCIを無理に通さない。
   - Lint、型チェック、静的解析を`// eslint-disable`や`# type: ignore`などで不当に握りつぶさない。
5. **不要コードの排除**
   - デバッグ用の一時コード、到達不能な死んだコード、不要なログ出力を残したまま完了としない。
6. **知らないことは当日リサーチ (Research Before Claiming)**
   - **人間**: AIが「覚えている風」に書いた古い知識が一番危ない。知らないこと・古いかもしれないことは、その日の最新情報を調べてから書かせる。
   - **AI**: 自分の知識に無い、または知識の期限より新しい可能性がある事柄（モデル・価格・API仕様・他社事例・日付つきの出来事）は、記憶で答えず**当日のWebリサーチで確認**してから書く。確認できたものには出典を付ける。
7. **曖昧なことは曖昧と書く (Mark Ambiguity)**
   - **人間**: 「はっきりしない」と書いてある報告のほうが、断言だらけの報告より信用できる。断言の顔をした推測が一番の毒。
   - **AI**: 未確認・推測・記憶頼み・出典間で食い違う記述には「未確認」「推測」「諸説あり」と明記し、検証済みの事実と混ぜない。曖昧さを消すために断定に丸めない。

---

# LEVEL B — DEVELOPMENT PRINCIPLES (開発原則)

すべての開発タスクにおける基本原則。

```text
1. Goalを確認する
2. repository内を先に探す (Search First)
3. Library / Framework / SDK / API / OSS / SaaS / 特化AIを探す (Reuse First)
4. ExistingとGoalの差分をGapとして定義する (Define the Gap)
5. Gapだけを実装する (Build Only the Gap)
6. 不要な抽象化をしない (No Unnecessary Abstraction)
7. 将来用途だけの実装をしない (No Speculative Implementation)
8. 変更範囲を必要最小限にする (Minimum Change)
9. 機械判定できるものは機械に任せる (Mechanical Check First)
10. 最後は実際に動いた事実で証明する (Runtime Evidence)
11. 想定外の値をそのまま返さず、その場で停止する。対象は 金額 / 日付 / 権限 / データの削除・上書き の4領域のみ (Fail Fast)
```
- 11 の根拠と適用範囲: （4領域以外は「動けばよい」とする）

### デフォルト鉄板構成 (Default Golden Stack)
新規構築や技術選定に特段の指定がない場合は、以下の**鉄板構成**をデフォルトとする。

| レイヤー | デフォルト技術スタック (Golden Stack) |
|---|---|
| **言語・ランタイム** | TypeScript (Strict Mode) / Node.js LTS (v20+) |
| **フレームワーク** | Next.js (App Router) / React |
| **スタイリング・UI** | Tailwind CSS + shadcn/ui + Lucide Icons |
| **API / バックエンド** | Next.js Route Handlers / Fastify (Python時は FastAPI) |
| **DB & ORM** | PostgreSQL (本番) / SQLite (PoC/ローカル), Drizzle ORM / Prisma |
| **バリデーション** | Zod (Schema-First Type Inference) |
| **テスト & 検証** | Vitest (Unit/Integration) + Playwright (E2E / Runtime Evidence) |
| **Linter / Formatter**| Biome (または ESLint + Prettier) |
| **CI / CD** | GitHub Actions |

### 標準開発Harness (Default Harness)

通常開発の基本フローは以下のみとする。ReviewやFailure Matchは条件付きでのみ追加する。

```text
EXPLORE  -->  BUILD  -->  VERIFY
```

---

# LEVEL C — RISK-BASED WORKFLOW (リスク別ワークフロー)

**工程の背骨は冒頭の3本柱（門①＝新しいゴールを決めるとき1回 / 常時掲示ゴール / 門②＝変更が出来上がった直後）である。** 本 LEVEL C の5ステップ（EXPLORE / FAILURE MATCH / BUILD / INDEPENDENT CRITIC / REAL VERIFICATION）は3本柱に並ぶ別系統ではなく、**STRICT と判定された変更にのみ適用する詳細工程**である。FAST では標準 Harness（`EXPLORE` → `BUILD` → `VERIFY`）と3本柱で足り、STEP 2 / STEP 4 / STEP 5 は起動しない。両者が食い違う場合は3本柱が優先する。

## 1. 判定軸は FAST / STRICT の1本 (Mode Judgement)

| モード | 対象 | 適用フロー | セットアップ最低要件 |
|---|---|---|---|
| **FAST** (基本) | Prototype, PoC, UI確認, 実験, 内部利用, 短期検証, typo・CSS微調整・単純rename・1行バグ修正, 単一API・小機能・局所バグ修正, 新機能追加, 複数ファイル変更, State管理, 外部API連携 | `EXPLORE` → `BUILD` → `MECHANICAL CHECK` → `VERIFY` | 起動可能, Lint, 最低限テスト, Git管理, README |
| **STRICT** | Production, 公開サービス, Auth/認可, 決済/課金, 個人データ, DBマイグレーション, セキュリティ基盤, データ削除, 秘密情報/クレデンシャル | `EXPLORE` → `FAILURE MATCH` → `BUILD` → `MECHANICAL CHECK` → `INDEPENDENT CRITIC` → `WRITER FIX` → `AUTOMATED TEST` → `RUNTIME VERIFY` → `INDEPENDENT VERIFIER` | FAST要件 ＋ CI, Branch Protection, Smoke/E2E, 独立検証, セキュリティ検査 |

- **判定キーワード**: `auth`, `payment`, `personal data`, `public release`, `production DB`, `migration` のいずれかを含む場合は自動で STRICT、それ以外は FAST を基本とする。**STRICT と判定したら、着手前に [`verified-plan` スキル](./.agents/skills/verified-plan/SKILL.md)（計画→敵対検証→受け入れ条件）を通す。**
- 旧リスクレベル判定（4段階）は廃止し、判定軸を FAST / STRICT の1本に統合した。根拠: 

---

## 2. 各ステップの詳細仕様

### STEP 1: EXPLORE (探索と差分定義)
- **目的**: 何を作るべきかではなく、**何だけを作ればよいか (GAP)** を特定する。
- **出力形式** (巨大なDiscovery Reportは作らない。原則この3点のみ):
  ```text
  GOAL:     [今回何を実現するか]
  EXISTING: [リポジトリ内・外部OSS等で既に存在・利用可能なもの]
  GAP:      [今回新しく実装・変更する最小差分]
  ```
- **省略条件**: FAST かつ変更内容を1文で説明でき、既存設計を変更しない場合はEXPLOREを省略可能。

---

### STEP 2: FAILURE MATCH (過去失敗照合)
- **未実走**（実開発リポジトリでの実行記録なし。2026-08-26 時点）
- **目的**: `docs/failures.md` と照合し、過去の失敗の再発を未然に防ぐ。
- **トリガー条件** (以下のいずれかに該当する場合のみ実行):
  1. **STRICT** と判定された変更
  2. **既知リスク領域 (Known-Risk Area)** の変更:
     `AGENTS.md`, `.claude/`, `.github/`, CI/CD, Git/ブランチ運用, PR/マージ, セットアップ, デプロイ, シークレット管理, テスト基盤
  3. **同一失敗の2回発生 (Repeated Failure)**:
     コマンド失敗、CI失敗、デプロイ失敗、プッシュ/マージ失敗、同一バグ修正の繰り返し、ユーザーからの2回以上の同種指摘
- **担当**: 独立軽量Sub-Agent `failure-matcher`
- **入力**: `GOAL`, `GAP`, `TOUCH` (触るファイル/サブシステム), `PLAN` (予定変更方針) のみ。全文は渡さない。
- **出力**: 最大3件 (`Failure`, `Why relevant`, `Guardrail`) または `NO RELEVANT FAILURE` のみ。

---

### STEP 3: BUILD & MECHANICAL CHECK (最小実装と機械検査)
- **BUILD原則**:
  - Reuse first / Minimum change / Existing pattern first
  - 禁止: 不要なFramework/wrapper、既存helperの再発明、将来用機能、不要な依存追加、全面rewrite、無関係なrefactor
  - 目標: 「最も美しい設計」ではなく「現在のGoalを満たす最小の整合した変更」
- **MECHANICAL CHECK**:
  - Lint, Typecheck, Unit Test, Integration Test, Build, Static Analysis, Smoke, E2E
  - 機械判定できるものは先にすべて自動実行する。LLM Reviewerに機械判定を任せない。

---

### STEP 4: INDEPENDENT CRITIC (独立レビュー)
- **未実走**（実開発リポジトリでの実行記録なし。2026-08-26 時点）
- **目的**: 作成者 (Writer) が気付かなかった重大問題を発見する。
- **トリガー**: **STRICT** と判定された変更のビルド・機械検査通過後
- **担当**: 独立した目線を持つ `independent-critic` (フレッシュコンテキスト)
- **確認項目 (3点のみ)**:
  1. `WRONG GOAL`: 本来の要求・UXを外していないか、必要なフローが抜けていないか、別の問題を解いていないか
  2. `WRONG APPROACH`: 既存解/OSSの車輪の再発明、不適切な技術選択、過剰実装・不要な抽象化がないか
  3. `REAL BUG`: Regression, データ損失, 状態不整合, Race Condition, 権限漏れ, Security, クラッシュ
- **制約**: Critic自身はコードを書かない。リファクタリングを要求しない。代案実装を書かない。
- **Writerの後処理**: 指摘を「採用」「却下」「追加調査」に分類し、採用する場合のみ最小修正する（全面書き直しは禁止）。

---

### STEP 5: REAL VERIFICATION (外部事実による実証)
- **原則**: 自己申告の推論ではなく、外部事実 (Runtime Evidence) で完了を証明する。
- **証拠の強さ**:
  `AI Reasoning < Code Review < Automated Test < Runtime Evidence`
- **対象別検証方法**:
  - **UI / Web**: 実ブラウザ / Playwright等での実操作と表示結果
  - **API**: 実際のHTTPリクエスト/レスポンスとDB状態
  - **Database**: マイグレーション実行、読み書き、ロールバック、互換性
  - **Auth / 権限**: 許可される正常系と**拒否されるべき異常系**の双方案件
  - **File / Batch**: 実ファイルの入出力処理と生成された成果物の内容検証
- **INDEPENDENT VERIFIER**: **未実走**（実開発リポジトリでの実行記録なし。2026-08-26 時点）
  - **STRICT** の最終完了前、または重要な公開・完了判定時に稼働。完了報告のコマンド・検証手順を再実行し、外部事実を確認する。

---

## 3. セッションの In / Out 規約

### セッション開始 (In)
1. `docs/handoff.md` を読む。
2. 必要に応じて `spec.md` と `docs/design.md` を読む。
3. `docs/failures.md` 全文は**読まない**（必要なときだけ Failure Matcher が参照）。

### セッション終了 (Out)
1. `docs/handoff.md` を更新する（今回やったこと、現在の状態、次回やること）。
2. 失敗が発生・解決した場合は `docs/failures.md` に追記する (append-only)。
3. コミット & プッシュ（PR / マージは指示がある場合のみ）。

---

# LEVEL D — REPOSITORY-SPECIFIC RULES (プロジェクト固有ルール)

本プロジェクト固有の制約・要件をここに追記する。

### ルール昇格原則 (Promotion Rule)
- 失敗が1件起きただけで安易にGlobal Rule（AGENTS.md）へ追加してはならない。
- 失敗はまず `docs/failures.md` に記録し、Failure Matcherで再利用する。
- **「複数回発生した」「重大事故に直結する」「機械判定可能」「汎用性が高い」** 条件を満たすもののみ、このセクションまたは上位レベルへ昇格する。

### [プロジェクト固有ルール一覧]

#### D-1. この案件のゴールは `spec.md` 1枚で固定する (Acceptance-First)
- 着手時に [`spec.md`](./spec.md) を埋め、門①で二択1回だけ確認して確定する。手順は [`project-spec` スキル](./.agents/skills/project-spec/SKILL.md)。spec が固定するのは**検収の下限であって期待値の上限ではない**。期待値が動いたら第1〜4節を更新する。
- **第2節の検収条件がそのまま E2E のハッピーパスになる**（[`templates/e2e/`](./templates/e2e/README.md)）。検査は [`scripts/check-test-integrity.sh`](./scripts/check-test-integrity.sh) と [`scripts/check-catastrophic.sh`](./scripts/check-catastrophic.sh)。CI 雛形は [`templates/ci/acceptance.yml`](./templates/ci/acceptance.yml)。

#### D-2. コミュニケーション規約 (Communication Protocol) — 永続指示
- **丁寧な言葉遣いを標準とする。** 断定的・命令的・ぶっきらぼうな口調を用いない。
- **比喩・たとえ話・回りくどい言い換えを禁止する。** 技術的な事象は**技術用語のまま、正確に**記述し、必要なら定義を添える。
- 曖昧な要約で誤魔化さず、事実・推測・提案を明確に区別して述べる。

#### D-3. 失敗の記録 (Failure Log)
- 作業中に発生し解決した失敗は [`docs/failures.md`](./docs/failures.md) の末尾に追記する (Append-Only)。過去の記録を書き換えない。
- 照合は必要なときだけ `failure-matcher` が行う。**全文を読み込まない**（コンテキスト汚染防止）。

### 迷子にならないためのルール (D-6〜D-11)

> 現行ルールの良い点（技術構成を決める器 / In-Out 引き継ぎ / 巨人の肩に乗る前提）は変更しない。不足していた「迷子にならない仕組み」をここで補う。

#### D-6. ルールは2行で書く (Two-Line Rule)
- **人間**: どのルールも「人間向け1行」と「AI向け1行」で書く。読めないルールは、発注者が判断も認識合わせもできない。
- **AI**: D-6 以降の新設・改訂ルールに適用する。`- **人間**:`（専門語を避け、必要なら定義を添える）と `- **AI**:`（ファイル名・コマンド・出典はこちらに置く）の2行構成。既存ルール（LEVEL A〜C, D-1〜D-5）への遡及適用は未決。

#### D-7. ルールの正は1つ (Single Source of Truth)
- **人間**: ルールを書く場所はこのファイル1か所だけ。2か所にあると、AIごとに言うことが変わる。
- **AI**: 正は `AGENTS.md` 一本。`CLAUDE.md` / `.cursorrules` 等のエージェント固有ファイルには「AGENTS.md に従え」のポインタのみを置き、ルール本文を書かない。機械強制は特定AIのフック機構ではなく、どのAIでも実行できるコマンド（`scripts/`）に落とす。

#### D-8. フェーズで区切り、範囲の外に出ない (Phase & Scope Discipline)
- **人間**: 大きい仕事は区切り（フェーズ）に割り、いまの区切りの目的を1行で決める。区切りの外は触らない。途中で見つけた問題は「直さないと先に進めないか？」の1問だけで判断し、原則あと回しの記録に残す。仕組みづくりに掛ける時間が成果物に掛ける時間を超えたら、それ自体が偽解決。
- **AI**: フェーズ開始前に「目的1行＋触ってよい場所の表」を確定する。範囲外のファイル変更・新規依存の追加をしない（AIの逸脱は「頼んだ範囲の外に出る」型が主である）。あと回しは記録に残し、全フェーズ完了後にユーザーが「直す／放置」を決める。scope guard / gate の機械化は後続の CI 設計時。

#### D-9. 「調べた」「直した」「解決した」を混同しない (True-Fix Evidence)
- **人間**: 「直した」と言うには証拠が3つ要る — 壊れていた証拠、直った証拠、直しを元に戻すと同じ壊れ方に戻る証拠。3つ揃わないものは「直した」ではない。
- **AI**: 解決の主張1件につき「赤（修正前に失敗する検査の出力）/ 緑（修正後）/ 戻して赤（revert で同一エラーが再現）」の3出力を提示する。検査は解決行動の**前**に作る（後から作ると、直したものしか捕まえられない検査になる）。①で別のエラーが出たなら直したのは別のもの。STEP 5 (Runtime Evidence) の具体化。

#### D-10. 偽解決カタログと照合する (Fake-Fix Catalog)
- **人間**: ずるして緑にする手口は12種類が表になっている。仕事の区切りごとにこの表へ当てて確かめ、新しい手口を見つけたら表に足す。
- **AI**: フェーズ境目に 参照資料 第5章のカタログと照合する。検査の本数・走査対象の件数を減らして緑化しない（LEVEL A-4 の具体化）。新手口の発見は原文の表ではなく `docs/failures.md` に記録し、複数回発生したらルールへ昇格する（Promotion Rule）。

#### D-11. 打ち手は制約5点で自己検査する (Constraint Self-Check)
- **人間**: 提案は出す前に「あなたが気づく・判断する・手を動かす」ことを当てにしていないか確かめる。当てにした案はすべて却下されてきた。
- **AI**: 提案前に制約5点 — ①依頼者は非エンジニア（コードを読めない） ②多数の案件を並列で回す ③24時間稼働 ④人が守るルールは0にする ⑤複数AI（Claude / Codex / Antigravity）が同一リポジトリを触る — のすべてに照合する。**人間の関与が案件数に比例する案は出さない。**
