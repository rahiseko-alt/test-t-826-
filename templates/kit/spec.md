# spec.md — 案件の要件（雛形）

> **この1枚を埋めることが要件定義です。分量ではなく、この5つの確度にエネルギーを注いでください。**

この雛形は案件リポジトリの直下に `spec.md` として置きます。記入は担当エージェントが行い、利用者に書かせません。
第1〜4節は契約（検収の基準）です。第5節はそれ以外の扱いを定めます。

## 1. 誰の何が良くなるか

1文で書きます。利用者の言葉で書き、技術用語・製品名・手段を書きません。

- 記入例: 店舗の店長が、月末に手作業で集計していた売上の締め作業を、画面の操作だけで終えられるようになります。
- 記入欄:

## 2. 検収条件

**この節に書いた通し操作は、そのまま E2E のハッピーパスになります。** 転写の手順と雛形は [`templates/e2e/README.md`](./templates/e2e/README.md) を参照してください。

書式は Given（前提の状態）/ When（人が行う操作）/ Then（画面で確認できる結果）です。1件が通し操作1本に対応します。最低1件、通常は1〜3件書きます。

- 記入例1: Given 未登録の利用者がトップページを開いている。When メールアドレスとパスワードを入力して「登録」を押す。Then 登録完了の画面が表示され、同じメールアドレスで再度ログインできる。
- 記入例2: Given 売上データが3件登録済みで、店長としてログインしている。When 期間に「今月」を選んで「集計する」を押す。Then 3件の合計金額が画面に表示され、CSV としてダウンロードできる。
- 記入例3: Given 予約が1件も無い状態で、利用者がログインしている。When 予約一覧の画面を開く。Then 「予約はありません」と表示され、エラー画面にならない。
- 記入欄:

## 3. スコープ境界

- **契約に含む**:
- **含まない**:
- **追加費用になる**:

## 4. 停止4領域の所在

この案件で **金額 / 日付 / 権限 / データの削除・上書き** を扱う箇所を書きます。該当が無い領域には「なし」と書きます。
（この4領域では、想定外の値をそのまま返さずその場で停止します。停止すれば通し操作が完了しなくなり、第2節の検収条件が捕まえます。）

- **金額**:
- **日付**:
- **権限**:
- **データの削除・上書き**:

### 保護対象ルート一覧

未ログインの利用者が到達できてはいけない URL・エンドポイントを列挙します。この一覧は破滅的脆弱性の検査スクリプトが入力として読み取るため、形式を守ってください。

形式: この見出しの直下に **1行1ルート**で `- <HTTPメソッド> <パス>` と書きます。メソッドは大文字、パスは `/` で始めます。注釈・空行・入れ子の箇条書きを混ぜません。該当が無い場合は `- NONE` の1行だけを書きます。

**下の `- NONE` を、この案件の実際のルート一覧に置き換えてください。** 1行につき「大文字のメソッド」と「スラッシュで始まるパス」を1つずつ、上の形式どおりに並べます。認証を持たない案件では `- NONE` のままにします。

- NONE

## 5. 可変層の宣言

第1〜4節以外は、実装側の裁量で変えてよい範囲です。画面の構成・技術選定・ファイル配置・実装の順序について、利用者への確認は行いません。

期待値が変わったときは第1〜4節を更新し、更新した内容を記録します。**更新は失敗ではありません。** 作ってみて分かったことの結果です。
